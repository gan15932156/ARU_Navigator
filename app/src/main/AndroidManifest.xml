<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arunavigator">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.androidlearningtutorials.googlemap.permission.MAPS_RECEIVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.Activities.NewVertexActivity"
            android:label="@string/title_activity_new_vertex"></activity>
        <activity android:name=".Activities.Activities.EditSpDesActivity" />
        <activity android:name=".Activities.Activities.NewSpDesActivity" />
        <activity android:name=".Activities.Activities.SpacificDesActivity" />
        <activity android:name=".Activities.Activities.EditToiletActivity" />
        <activity android:name=".Activities.Activities.NewToiletActivity" />
        <activity android:name=".Activities.Activities.ManageToiletActivity" />
        <activity android:name=".Activities.Activities.EditFloorActivity" />
        <activity android:name=".Activities.Activities.NewFloorActivity" />
        <activity android:name=".Activities.Activities.ManageFloorActivity" />
        <activity android:name=".Activities.Activities.EditActActivity" />
        <activity android:name=".Activities.Activities.NewActActivity" />
        <activity android:name=".Activities.Activities.ManageAcActivity" />
        <activity android:name=".Activities.Activities.EditLocationActivity" />
        <activity
            android:name=".Activities.Activities.ChoosePositionActivity"
            android:label="@string/title_activity_choose_position" />
        <activity android:name=".Activities.Activities.NewLocationActivity" />
        <activity android:name=".Activities.Activities.ManageLocationActivity" />
        <activity android:name=".Activities.Activities.IndexAdminActivity" />
        <activity android:name=".Activities.Activities.AdminLoginActivity" />
        <activity
            android:name=".Activities.Activities.Show_acActivity"
            android:noHistory="true" />
        <activity android:name=".Activities.Activities.QR_code_scan" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".Activities.Activities.MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>